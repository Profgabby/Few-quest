"use client";import {usePathname,useRouter} from "next/navigation";import {localeNames,locales,type Locale} from "@/lib/i18n";export function LanguageSwitcher({locale}:{locale:Locale}){const path=usePathname();const router=useRouter();function change(code:Locale){const parts=path.split("/");parts[1]=code;router.push(parts.join("/")||`/${code}`)}return <div className="langs" aria-label="Language">{locales.map(code=><button key={code} className={code===locale?"active":""} onClick={()=>change(code)} lang={code} dir={code==="ar"?"rtl":"ltr"}>{localeNames[code]}</button>)}</div>}