"use client";import {useState} from "react";export function TeamAccessButton({membershipId,active}:{membershipId:string;active:boolean}){const [busy,setBusy]=useState(false);async function run(){setBusy(true);const r=await fetch("/api/school/team/access",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({membershipId,active:!active})});if(r.ok)location.reload();else setBusy(false)}return <button className={active?"btn btn-outline":"btn"} onClick={run} disabled={busy}>{busy?"Working…":active?"Deactivate":"Reactivate"}</button>}