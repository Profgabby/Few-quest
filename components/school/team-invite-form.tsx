"use client";import {useState} from "react";export function TeamInviteForm({locale}:{locale:string}){const [state,setState]=useState("");async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setState("Sending invitation…");const fd=new FormData(e.currentTarget);const r=await fetch("/api/school/team/invite",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({fullName:fd.get("fullName"),email:fd.get("email"),role:fd.get("role"),locale})});const j=await r.json();if(!r.ok){setState(j.message||j.error||"Could not add team member.");return}setState("Invitation sent and team access prepared.");setTimeout(()=>location.reload(),700)}return <form className="team-form" onSubmit={submit}><label>Full name<input name="fullName" required minLength={2}/></label><label>Email<input name="email" type="email" required/></label><label>Role<select name="role" defaultValue="teacher"><option value="teacher">Teacher</option><option value="competitor_manager">Competitor manager</option></select></label><button className="btn" type="submit">Invite team member</button>{state&&<p role="status">{state}</p>}</form>}