"use client";import {useState} from "react";
export function TeamInviteForm({locale}:{locale:string}){const [state,setState]=useState("");const [setupLink,setSetupLink]=useState<string|null>(null);async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setState("Creating team member…");setSetupLink(null);const fd=new FormData(e.currentTarget);const r=await fetch("/api/school/team/invite",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({fullName:fd.get("fullName"),email:fd.get("email"),phone:fd.get("phone"),role:fd.get("role"),preferredLanguage:fd.get("preferredLanguage"),locale})});const j=await r.json();if(!r.ok){setState(j.message||j.error||"Could not add team member.");return}setState("Team member added. Send the secure account setup link to the staff member.");setSetupLink(j.setupLink||null);setTimeout(()=>location.reload(),1800)}return <form className="team-form" onSubmit={submit}><label>Full name<input name="fullName" required minLength={2}/></label><label>Email<input name="email" type="email" required/></label><label>Phone <span>(optional)</span><input name="phone" type="tel"/></label><label>Role<select name="role" defaultValue="teacher"><option value="teacher">Teacher</option><option value="competitor_manager">Competitor manager</option></select></label><label>Preferred language<select name="preferredLanguage" defaultValue="en"><option value="en">English</option><option value="ha">Hausa</option><option value="yo">Yorùbá</option><option value="ig">Igbo</option><option value="fr">Français</option><option value="ar">العربية</option></select></label><button className="btn" type="submit">Add team member</button>{state&&<p role="status">{state}</p>}{setupLink&&<p><a href={setupLink} target="_blank" rel="noreferrer">Open secure password setup link</a></p>}</form>}