import Link from "next/link";import {notFound} from "next/navigation";import {isLocale} from "@/lib/i18n";import {LanguageSwitcher} from "@/components/language-switcher";import {AdminSetupForm} from "@/components/auth/admin-setup-form";export default async function AdminSetup({params}:{params:Promise<{locale:string}>}){const {locale}=await params;if(!isLocale(locale))notFound();return <main><nav className="nav"><Link className="brand" href={`/${locale}`}>LIFEWS • FEW QUEST</Link><LanguageSwitcher locale={locale}/></nav><div className="auth-page"><section><div className="eyebrow">LIFEWS HQ</div><h1>Administrator<br/>setup.</h1><p className="lead">Create the authorized LIFEWS HQ identity separately from school registration.</p><Link href={`/${locale}/login`}>← School login</Link></section><AdminSetupForm locale={locale}/></div></main>}