import Link from "next/link";import {notFound} from "next/navigation";import {isLocale} from "@/lib/i18n";import {RegisterForm} from "@/components/auth/register-form";export default async function Register({params}:{params:Promise<{locale:string}>}){const {locale}=await params;if(!isLocale(locale))notFound();return <main className="auth-page"><section><div className="eyebrow">Join FEW Quest</div><h1>Register<br/>your school.</h1><p className="lead">Create one verified school identity for GrowMeal, GrowEnergy/GrowVolt and GrowAqua competitions.</p><Link href={`/${locale}`}>← FEW Quest home</Link></section><RegisterForm locale={locale}/></main>}