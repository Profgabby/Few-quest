import Link from "next/link";import {isLocale} from "@/lib/i18n";import {notFound} from "next/navigation";export default async function Register({params}:{params:Promise<{locale:string}>}){const {locale}=await params;if(!isLocale(locale))notFound();return <main className="hero"><div><div className="eyebrow">Phase 1</div><h1>School<br/>Registration</h1><p className="lead">The secure registration workflow will connect here to the isolated FEW Quest Supabase project. No existing GrowMeal production data is used.</p><div className="actions"><Link className="btn" href={`/${locale}`}>Back to FEW Quest</Link></div></div></main>}
