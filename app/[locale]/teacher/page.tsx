import {redirect} from "next/navigation";import {createClient} from "@/lib/supabase/server";import {isLocale} from "@/lib/i18n";import {notFound} from "next/navigation";
export default async function Teacher({params}:{params:Promise<{locale:string}>}){const {locale}=await params;if(!isLocale(locale))notFound();const s=await createClient();const {data:{user}}=await s.auth.getUser();if(!user)redirect(`/${locale}/login`);return <main className="hero"><div><div className="eyebrow">Teacher Workspace</div><h1>Prepare.<br/>Teach. Compete.</h1><p className="lead">Team preparation and learning tools will appear here as FEW Quest phases are activated.</p></div></main>}
