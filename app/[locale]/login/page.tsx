import Link from "next/link";import {notFound} from "next/navigation";import {isLocale} from "@/lib/i18n";import {getMessages} from "@/messages";import {LoginForm} from "@/components/auth/login-form";import {LanguageSwitcher} from "@/components/language-switcher";export default async function Login({params}:{params:Promise<{locale:string}>}){const {locale}=await params;if(!isLocale(locale))notFound();const t=getMessages(locale);return <main><nav className="nav"><Link className="brand" href={`/${locale}`}>LIFEWS • FEW QUEST</Link><LanguageSwitcher locale={locale}/></nav><div className="auth-page"><section><div className="eyebrow">{t.schoolAccess}</div><h1>{t.welcomeBack}</h1><p className="lead">{t.platform}</p><Link href={`/${locale}`}>← FEW Quest</Link></section><LoginForm locale={locale}/></div></main>}