import Link from "next/link";import {isLocale} from "@/lib/i18n";import {notFound} from "next/navigation";export default async function Login({params}:{params:Promise<{locale:string}>}){const {locale}=await params;if(!isLocale(locale))notFound();return <main className="hero"><div><div className="eyebrow">Phase 1</div><h1>School<br/>Login</h1><p className="lead">Authentication will be enabled against the dedicated FEW Quest Supabase project in the next foundation step.</p><div className="actions"><Link className="btn" href={`/${locale}`}>Back to FEW Quest</Link></div></div></main>}
