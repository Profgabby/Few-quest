import Link from "next/link";import {notFound} from "next/navigation";import {isLocale} from "@/lib/i18n";import {LoginForm} from "@/components/auth/login-form";export default async function Login({params}:{params:Promise<{locale:string}>}){const {locale}=await params;if(!isLocale(locale))notFound();return <main className="auth-page"><section><div className="eyebrow">FEW Quest School Access</div><h1>Welcome<br/>back.</h1><p className="lead">Sign in to your school workspace, preparation tools and competitions.</p><Link href={`/${locale}`}>← FEW Quest home</Link></section><LoginForm locale={locale}/></main>}